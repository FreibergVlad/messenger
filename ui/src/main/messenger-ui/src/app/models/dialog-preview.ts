export class DialogPreview {

  userId: number;
  username: string;
  lastMessage: string;
  timestamp: number;

  constructor(userId, username, lastMessage, timestamp) {
    this.userId = userId;
    this.username = name;
    this.lastMessage = lastMessage;
    this.timestamp = timestamp;
  }

}
