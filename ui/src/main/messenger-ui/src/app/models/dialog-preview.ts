export class DialogPreview {

  userId: number;
  username: string;
  lastMessage: string;

  constructor(userId, username, lastMessage) {
    this.userId = userId;
    this.username = name;
    this.lastMessage = lastMessage;
  }

}
